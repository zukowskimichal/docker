sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t zukowskimichal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true zukowskimichal/docker-react npm run test
